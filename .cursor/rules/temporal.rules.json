{
  "rules": [
    {
      "name": "Allow all Temporal operations",
      "description": "Allow all operations on Temporal API",
      "path": "/",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "allow": true
    }
  ]
} 