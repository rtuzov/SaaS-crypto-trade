{
  "rules": [
    {
      "name": "Allow all Prometheus operations",
      "description": "Allow all operations on Prometheus API",
      "path": "/",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "allow": true
    }
  ]
} 