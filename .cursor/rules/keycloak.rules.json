{
  "rules": [
    {
      "name": "Allow all Keycloak operations",
      "description": "Allow all operations on Keycloak API",
      "path": "/",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "allow": true
    }
  ]
} 