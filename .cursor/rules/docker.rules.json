{
  "rules": [
    {
      "name": "Allow all Docker operations",
      "description": "Allow all operations on Docker API",
      "path": "/",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "allow": true
    }
  ]
} 