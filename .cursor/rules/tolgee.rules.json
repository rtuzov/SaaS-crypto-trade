{
  "rules": [
    {
      "name": "Allow all Tolgee operations",
      "description": "Allow all operations on Tolgee API",
      "path": "/",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "allow": true
    }
  ]
} 