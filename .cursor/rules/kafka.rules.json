{
  "rules": [
    {
      "name": "Allow all Kafka operations",
      "description": "Allow all operations on Kafka topics",
      "path": "/",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "allow": true
    }
  ]
} 