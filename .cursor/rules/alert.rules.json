{
  "rules": [
    {
      "name": "Allow all Alertmanager operations",
      "description": "Allow all operations on Alertmanager API",
      "path": "/",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "allow": true
    }
  ]
} 