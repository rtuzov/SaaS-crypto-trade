{
  "rules": [
    {
      "name": "Allow all database operations",
      "description": "Allow all operations on the trading database",
      "path": "/",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "allow": true
    }
  ]
} 